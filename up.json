{
  "name": "survey-api",
  "profile": "licketybit",
  "regions": ["us-west-2"],
  "lambda": {
    "memory": 128
  },
  "stages": {
    "staging": {
      "hooks": {
        "build": "GOOS=linux GOARCH=amd64 go build -o server cmd/api/main.go",
        "clean": "rm server"
      }
    }
  }
}
